<%- include('partials/header', { title: className }) %>
<h1>Посты: <%= className %></h1>
<% if (!posts.length) { %>
  <p>Бұл сыныпта пост жоқ.</p>
<% } else { %>
  <% posts.forEach(p => { %>
    <article>
      <h2><%= p.title %></h2>
      <p><%= p.body %></p>
      <small><%= p.createdAt.toLocaleString() %></small>
    </article>
    <hr>
  <% }) %>
<% } %>
<p><a href="/" class="btn">← Артқа</a></p>
<%- include('partials/footer') %>
