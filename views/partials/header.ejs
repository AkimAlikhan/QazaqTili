<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin:0; padding:0; }
    body { font-family:'Open Sans',sans-serif; font-size:16px; color:#333; background:#fff; line-height:1.6; padding:2rem; }
    nav ul { list-style:none; display:flex; gap:1rem; }
    nav a { text-decoration:none; color:#333; padding:0.5rem; transition:color .2s; }
    nav a:hover { color:#007acc; }
    .btn { display:inline-block; padding:.5rem 1rem; border:2px solid #007acc; background:transparent; color:#007acc; cursor:pointer; font-weight:600; transition: background .2s, color .2s; }
    .btn:hover { background:#007acc; color:#fff; }
    input, textarea, select { width:100%; padding:.5rem; border:1px solid #ccc; margin-bottom:1rem; }
    .container { max-width:800px; margin:auto; }
    h1,h2,h3 { margin-bottom:1rem; font-weight:600; }
    p, li { margin-bottom:1rem; }
    hr { border:none; border-top:1px solid #eee; margin:2rem 0; }
    @media(max-width:600px){body{padding:1rem;}nav ul{flex-direction:column;gap:.5rem;}}
  </style>
</head>
<body>
  <nav class="container">
    <ul>
      <% classes.forEach(c => { %>
        <li><a href="/class/<%= encodeURIComponent(c) %>"><%= c %></a></li>
      <% }) %>
      <li><a href="/login" class="btn">Admin</a></li>
    </ul>
  </nav>
  <main class="container">
